import { Fonts } from "@/app/components/fonts";
import styled from "styled-components/native";

export const ExploreWrapper = styled.View``;

export const ExploreHeader = styled.Text`
  font-family: ${Fonts.rubikBold};
  margin: 10px 0;
  font-weight: bold;
  font-size: 30px;
`;
